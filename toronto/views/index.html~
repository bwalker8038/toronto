<!DOCTYPE html>
<html>

  <head>
    <title>Backbone Demo: Messages</title>
    <link href="messages.css" media="all" rel="stylesheet" type="text/css"/>
     
    <script src="/jquery.min.js"></script>
    <script src="/underscore-min.js"></script>
    <script src="/backbone.js"></script>
    <script src="/dnode.js"></script>
    <script src="/dnode.backbone.js"></script>
    <script src="/messages.js"></script>
  </head>

  <body>

    <!-- Message App Interface -->

    <div id="messageapp">

      <div class="title">
        <h1>Messages</h1>
      </div>

      <div class="content">

        <div id="create-message">
          <input id="new-message" placeholder="What needs to be done?" type="text" />
          <span class="ui-tooltip-top" style="display:none;">Press Enter to save this task</span>
        </div>

        <div id="messages">
          <ul id="message-list"></ul>
        </div>

        <div id="message-stats"></div>

      </div>

    </div>

    <ul id="instructions">
      <li>Double-click to edit a message.</li>
    </ul>

    <div id="credits">
      Created by
      <br />
      <a href="http://jgn.me/">J&eacute;r&ocirc;me Gravel-Niquet</a>
      
      <br />
      <br />
      Model persistence by
      <br />
      <a href="http://beausorensen.com/">Beau Sorensen</a>
    </div>

    <!-- Templates -->

    <script type="text/template" id="item-template">
      <div class="message <%= done ? 'done' : '' %>">
        <div class="display">
          <input class="check" type="checkbox" <%= done ? 'checked="checked"' : '' %> />
          <div class="message-content"></div>
          <span class="message-destroy"></span>
        </div>
        <div class="edit">
          <input class="message-input" type="text" value="" />
        </div>
      </div>
    </script>

    <script type="text/template" id="stats-template">
      <% if (total) { %>
        <span class="message-count">
          <span class="number"><%= remaining %></span>
          <span class="word"><%= remaining == 1 ? 'item' : 'items' %></span> left.
        </span>
      <% } %>
      <% if (done) { %>
        <span class="message-clear">
          <a href="#">
            Clear <span class="number-done"><%= done %></span>
            completed <span class="word-done"><%= done == 1 ? 'item' : 'items' %></span>
          </a>
        </span>
      <% } %>
    </script>

  </body>

</html>
